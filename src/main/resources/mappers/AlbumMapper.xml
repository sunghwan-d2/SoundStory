<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ksh.soundstory.mappers.Al">
    <select id="selectAlbumByIndex" resultType="com.ksh.soundstory.entities.AlbumEntity">
        SELECT `album_id`           AS `albumId`,
               `image_data`         AS `imageData`,
               `image_content_type` AS `imageContentType`,
               `image_file_name`    AS `imageFileName`,
               `title`              AS `title`,
               `song_id`            AS `songId`,
               `artist_id`          AS `artistId`
        FROM `ss`.`album`
        WHERE `album_id` = #{albumId}
    </select>



</mapper>
